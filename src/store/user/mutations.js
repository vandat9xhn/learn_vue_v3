//
const storeUserMutations = {
    fetchUser(state, payload) {
        // state.first_name = payload.first_name;
        // state.last_name = payload.last_name;
    },
};

export default storeUserMutations;
